{
  "name": "signal-root",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "7zip-bin": "^5.2.0",
    "app-builder-bin": "^4.0.0",
    "async-exit-hook": "^2.0.1"
  },
  "scripts": {
    "dev": "npm run dev -w apps/desktop",
    "build": "npm run build -w apps/desktop",
    "lint": "npm run lint -w apps/desktop",
    "format": "npm run format -w apps/desktop"
  },
  "devDependencies": {
    "electron-rebuild": "^3.2.9"
  }
}
